@model CinemaClient.Domain.CinemaRoom

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>CinemaRoom</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Capacity)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Capacity)
        </dd>
        <dt class = "col-sm-2">
            Film
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CurrentScreening.Movie.Title)
        </dd>
    </dl>
</div>
@if (Model.CurrentScreening is not null)
{
    @await Html.PartialAsync("ScreeningDetails", Model.CurrentScreening)
}
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
